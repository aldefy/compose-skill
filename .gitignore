# Superpowers specs and plans (local workflow artifacts)
docs/superpowers/

# Git worktrees (if created inside the repo)
.worktrees/
worktrees/

# Claude local settings (never committed)
**/.claude/settings.local.json

# Local-only agent guidance (never committed)
CLAUDE.md
AGENTS.md

# Gradle / verify-claims module
.gradle/
build/
verify-claims/build/
buildSrc/build/
*.iml
local.properties

# macOS
.DS_Store
